# Setup

- `cd api && pnpm i`
- `cd client && pnpm i`

# Start

- `cd api && pnpm start:dev`
- `cd client && pnpm dev`

# CodeGen

> Inside client folder, and the api has to be run


- `pnpm codegen`
